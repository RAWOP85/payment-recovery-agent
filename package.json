{
  "name": "payment-recovery-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Agent that detects failed/abandoned Razorpay test-mode payments, diagnoses a recovery strategy via LLM (deterministic rule-table fallback) gated by a policy engine, escalates nudges on a fixed schedule, and reports honest recovered/unrecovered outcomes with a full audit trail.",
  "type": "commonjs",
  "scripts": {
    "check:razorpay": "node scripts/check-razorpay.js",
    "generate:dataset": "node data/generate-dataset.js",
    "test": "node --test",
    "recover": "node scripts/run-recovery.js",
    "evaluate": "node scripts/run-evaluation.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.4.2",
    "razorpay": "^2.9.8"
  }
}
